# Screencasts

`preview.cast` is an asciinema-compatible text screencast that demonstrates
`ozsh preview` and `ozsh theme preview`. Convert it to GIF with:

```bash
agg docs/screencasts/preview.cast docs/screencasts/preview.gif
```

